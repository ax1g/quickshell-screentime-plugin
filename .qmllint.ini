[General]
DisableDefaultImports=false
MaxWarnings=-1

# omarchy-shell types several runtime APIs as plain QtObject (Style.font,
# bar.*), so their members are invisible to the linter although they
# resolve at runtime (verified: the panel renders). Keep the category
# visible as info so genuine typos still print, without failing builds.
[Warnings]
MissingProperty=info
